ROUTES (/chats):

1. PATCH (if user exist in chat and this is user who requested) /leave/:chatId
2. PATCH (only creator can invite && user cannot be already in chat) /invite
3. PATCH (only creator can kick out && user has to be in chat && creator cannot kick out himself) /kick-out

1. POST (create new chat) /
2. GET (single chat) /:chatId
3. GET (get all chats) /
4. PATCH (only creator can update title) /?title={}
5. DELETE (only creator can update) /chatId